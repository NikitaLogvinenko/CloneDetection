<UserControl x:Class="WpfCodeMetaDataProcessorVisualization.UserControls.FileStorageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfCodeMetaDataProcessorVisualization.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView x:Name="Viewer" MouseLeftButtonUp="PreviewMouseLeftButtonDown" ItemsSource="{Binding GetSystem}" Background="LightBlue">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Files" DisplayMemberBinding="{Binding Key}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
